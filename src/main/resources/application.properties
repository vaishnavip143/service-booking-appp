# Use H2 in-memory database for development (easy to run without installing MySQL)
spring.datasource.url=jdbc:h2:mem:service_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# If you prefer MySQL, uncomment and configure the lines below and remove the H2 lines above
# spring.datasource.url=jdbc:mysql://localhost:3306/service_db?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=your_password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
